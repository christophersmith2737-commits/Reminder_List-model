# TEMPLATE SPEC · 清单模板规范（AI 与开发者通用）

> 目标：任何 AI 或用户按本规范生成的清单，都可以直接导入「清单追踪」类 APP，
> 且不会丢失条目。本文件是唯一权威规则；模板文件均由此规范推导。
>
> **AI 使用说明**：用户让你生成清单时，先问清主题与场景（购物/任务），
> 然后按对应模板的规则输出 CSV 表格。

## 1. 优先级体系

### 1.1 三档制（购物清单默认）

| 档位 | 权重 | 同义词（APP 全部兼容） | 英文 |
|------|------|------------------------|------|
| 必需 | 3 | 必须 | required |
| 推荐 | 2 | 建议 | suggested |
| 适用 | 1 | 日常 | daily |

### 1.2 星级制（工作流默认）

1~5 颗星表示重要程度，导入 APP 时自动映射到三档：

| 星数 | 写法示例 | 映射档位 | 权重 |
|------|----------|----------|------|
| 5★ | ★★★★★ / ⭐⭐⭐⭐⭐ / 5 | 必需 | 3 |
| 4★ | ★★★★ / ⭐⭐⭐⭐ / 4 | 必需 | 3 |
| 3★ | ★★★ / ⭐⭐⭐ / 3 | 推荐 | 2 |
| 2★ | ★★ / ⭐⭐ / 2 | 推荐 | 2 |
| 1★ | ★ / ⭐ / 1 | 适用 | 1 |

规则：
- 每行必须且只能有一个优先级；**推荐使用标准写法**（三档词或星号串）
- 无法识别的优先级值 = 该行作废（导入时被跳过并计数）

## 2. 模板一：购物清单（shopping-list）

### 2.1 Excel 结构（5 列）

| 列 | 表头 | 必填 | 说明 |
|----|------|------|------|
| A | 序号 | 否 | 从 1 递增；导入时自动忽略 |
| B | 物品名称 | **是** | 唯一必填文本列；为空则该行作废 |
| C | 数量 | 否 | 带单位（如：2瓶、1把、若干）；导入后拼到名称后显示为「名称 ×数量」 |
| D | 备注 | 否 | 用处、放置位置、注意事项等 |
| E | 优先级 | **是** | 三档之一（见 1.1） |

### 2.2 CSV 格式

表头（中文或英文均可，APP 自动识别）：

```csv
序号,物品名称,数量,备注,优先级
1,身份证,1张,报到注册需要，随身携带,必需
2,录取通知书,1份,放在随身背包，不要托运,必需
3,充电宝,1个,路上手机续航,推荐
```

等价英文表头：`name,quantity,note,priority`（序号列可省）。

### 2.3 JSON 格式

```json
{
  "title": "开学购买清单",
  "items": [
    { "name": "身份证", "quantity": "1张", "note": "报到注册需要，随身携带", "priority": "必需" },
    { "name": "充电宝", "quantity": "1个", "note": "路上手机续航", "priority": "推荐" }
  ]
}
```

## 3. 模板二：工作流（workflow）

**用于任务/计划类清单**，结构与购物清单不同：
- 第 1 行：任务总名称（作为清单标题）
- 第 2 行：表头 `任务序号 | 优先级 | 任务名称 | 任务备注`
- 第 3 行起：每个任务一行

### 3.1 Excel 结构（4 列）

| 列 | 表头 | 必填 | 说明 |
|----|------|------|------|
| A | 任务序号 | 否 | 从 1 递增；导入时自动忽略 |
| B | 优先级 | **是** | 1~5 颗星（见 1.2），建议下拉选择 |
| C | 任务名称 | **是** | 做什么事；为空则该行作废 |
| D | 任务备注 | 否 | 可写 2~3 行：截止时间（如「周五 18:00 前交」）、任务细节、交付标准；Excel 内 Alt+Enter 换行 |

### 3.2 CSV 格式

```csv
毕业论文工作流
任务序号,优先级,任务名称,任务备注
1,★★★★★,开题报告,"周五 18:00 前提交导师
需包含研究背景与时间规划"
2,★★★★,实验方案设计,明确对比实验与评估指标
3,★★★,中期报告,月底前完成初稿
```

注意：
- **首行是总名称**，不是表头；第二行才是表头（APP 自动识别）
- 星级优先级支持：`★★★★★` / `5★` / `5星` / `⭐5` / 数字 `5`
- 备注含换行时，整个字段用**双引号**包裹，内部换行原样保留

### 3.3 JSON 格式

```json
{
  "title": "毕业论文工作流",
  "items": [
    { "name": "开题报告", "priority": "★★★★★", "note": "周五 18:00 前提交导师\n需包含研究背景与时间规划" },
    { "name": "中期报告", "priority": "★★★", "note": "月底前完成初稿" }
  ]
}
```

## 4. 行排序规则（锚定优先级）

1. 优先级高的写在前面：购物清单按 必需→推荐→适用；工作流按 5★→4★→3★→2★→1★
2. 同档位内部按逻辑顺序（使用顺序 / 重要性 / 时间先后）排列
3. APP 追问时会自动按优先级排序；模板排序仅为人工阅读与生成规范

## 5. 数据校验规则（APP 导入时）

- 名称缺失 → 该行跳过，计入「跳过 N 条」
- 优先级无法识别 → 该行跳过，计入「跳过 N 条」
- 全部行无效 → 导入失败，提示用户检查格式
- 成功导入 → 提示「共 N 项（跳过 M 条无效数据）」，由用户确认后开始逐项追问

## 6. 生成清单时给 AI 的指令模板

### 6.1 购物清单

> 你是清单规划助手。请为用户生成一份「{主题}」购买清单，遵守：
> 1. 输出 CSV，第一行为表头 `序号,物品名称,数量,备注,优先级`；
> 2. 每行 5 列，逗号分隔；含逗号的字段用双引号包裹；
> 3. 优先级只能取：必需 / 推荐 / 适用；
> 4. 先输出全部「必需」，再「推荐」，最后「适用」；
> 5. 数量建议带单位（如 2瓶、1把）；条数 5~20 条。

### 6.2 工作流

> 你是任务规划助手。请为用户生成一份「{主题}」工作流，遵守：
> 1. 输出 CSV，**第一行为任务总名称**，第二行为表头 `任务序号,优先级,任务名称,任务备注`；
> 2. 优先级用 1~5 颗星（★），5★ 最重要；
> 3. 任务名称简明；任务备注可写截止时间与细节，需要换行时整段用双引号包裹；
> 4. 先写 5★，再 4★、3★、2★，最后 1★；
> 5. 任务数 3~15 个，覆盖完整流程。

## 7. 版本历史

- v1.0：购物清单模板 + 导入规范
- v1.1：工作流模板（首行总名称 + 星级优先级）+ 表头自动识别增强
