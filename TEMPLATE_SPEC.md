# TEMPLATE SPEC · 清单模板规范（AI 与开发者通用）

> 目标：任何 AI 或用户按本规范生成的清单，都可以直接导入「清单追踪」类 APP，
> 且不会丢失条目。本文件是唯一权威规则；模板文件均由此规范推导。

## 1. 优先级（三档，权重固定）

| 标准词 | 权重 | 同义词（APP 全部兼容） | 英文 | 数字 |
|--------|------|------------------------|------|------|
| 必需 | 3 | 必须 | required | 3 |
| 推荐 | 2 | 建议 | suggested | 2 |
| 适用 | 1 | 日常 | daily | 1 |

规则：
- 每行必须且只能有一个优先级，**推荐使用标准词**（必需/推荐/适用），
  避免同义词混用导致阅读歧义。
- 无法识别的优先级值 = 该行作废（导入时被跳过并计数）。

## 2. 表结构（Excel 模板为 5 列）

| 列 | 表头（模板） | 必填 | 说明 |
|----|--------------|------|------|
| A | 序号 | 否 | 从 1 递增；导入时自动忽略 |
| B | 物品名称 | **是** | 唯一必填文本列；为空则该行作废 |
| C | 数量 | 否 | 建议带单位（如：2瓶、1把、若干）；导入后拼到名称后，显示为「名称 ×数量」 |
| D | 备注 | 否 | 用处、放置位置、注意事项等，建议 20 字以内简洁描述 |
| E | 优先级 | **是** | 三档之一（见第 1 节） |

## 3. 行排序规则（锚定优先级）

1. **先写全部「必需」，再「推荐」，最后「适用」**（优先级降序）。
2. 同档位内部按逻辑顺序（使用顺序 / 重要性 / 放置顺序）排列。
3. APP 追问时会自动按 必需→推荐→适用 排序；模板排序仅为人工阅读与生成规范。

## 4. 导入格式映射（CSV / JSON）

### 4.1 CSV（推荐，Excel 直接另存为 UTF-8 即可）

- 编码：UTF-8（建议带 BOM，Windows Excel 兼容；APP 两种都支持）
- 分隔符：逗号；含逗号的备注用双引号包裹
- 表头：**中文表头与英文表头等价**，APP 自动识别：

| 模板表头 | CSV 表头（中文） | CSV 表头（英文） |
|----------|------------------|------------------|
| 序号 | 序号 | —（可省） |
| 物品名称 | 物品名称 / 名称 | name |
| 数量 | 数量 | quantity |
| 备注 | 备注 / 说明 | note |
| 优先级 | 优先级 / 等级 | priority |

示例（中文表头）：

```csv
序号,物品名称,数量,备注,优先级
1,身份证,1张,报到注册需要，随身携带,必需
2,录取通知书,1份,放在随身背包，不要托运,必需
3,充电宝,1个,路上手机续航,推荐
4,雨伞,1把,放宿舍门口，雨天用,适用
```

示例（英文表头，等价）：

```csv
name,priority,note,quantity
身份证,必需,报到注册需要，随身携带,1张
```

### 4.2 JSON

两种结构都支持：

```json
{
  "title": "开学购买清单",
  "items": [
    { "name": "身份证", "quantity": "1张", "note": "报到注册需要，随身携带", "priority": "必需" },
    { "name": "充电宝", "quantity": "1个", "note": "路上手机续航", "priority": "推荐" }
  ]
}
```

或顶层直接为 items 数组（无 title 时使用导入文件名作为清单名）。

字段与 CSV 列一一对应：`name`（必填）、`priority`（必填）、`note`（可选）、`quantity`（可选）。

## 5. 数据校验规则（APP 导入时）

- 名称缺失 → 该行跳过，计入「跳过 N 条」
- 优先级无法识别 → 该行跳过，计入「跳过 N 条」
- 全部行无效 → 导入失败，提示用户检查格式
- 成功导入 → 提示「共 N 项（跳过 M 条无效数据）」，由用户确认后开始逐项追问

## 6. 生成清单时给 AI 的指令模板

> 你是清单规划助手。请为用户生成一份「{主题}」清单，遵守以下规则：
> 1. 输出格式：CSV，第一行为表头 `序号,物品名称,数量,备注,优先级`；
> 2. 每行 5 列，列内容用逗号分隔；备注含逗号时整体用双引号包裹；
> 3. 优先级只能取：必需 / 推荐 / 适用；数量建议带单位；
> 4. 先输出全部「必需」行，再「推荐」，最后「适用」；
> 5. 条数建议 5~20 条，名称具体、备注简洁。

## 7. 版本历史

- v1.0：首个版本，购买清单模板落地（2025）
